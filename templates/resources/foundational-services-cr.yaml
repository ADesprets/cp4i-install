apiVersion: operator.ibm.com/v3
kind: CommonService
metadata:
  name: ${MY_COMMONSERVICES_INSTANCE_NAME}
  namespace: ${MY_COMMONSERVICES_NAMESPACE}
spec:
  operatorNamespace: ${MY_COMMONSERVICES_NAMESPACE}
  servicesNamespace: ${MY_COMMONSERVICES_NAMESPACE}
  installPlanApproval: Automatic
  size: starterset
  license:
    accept: true	
  storageClass: ${OCP_BLOCK_STORAGE}