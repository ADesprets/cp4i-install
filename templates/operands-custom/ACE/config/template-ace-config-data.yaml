apiVersion: appconnect.ibm.com/v1beta1
kind: Configuration
metadata:
  name: ${CONFIG_NAME}
  namespace: ${CONFIG_NS}
spec:
  type: ${CONFIG_TYPE}
  description: ${CONFIG_DESCRIPTION}
  data: ${CONFIG_DATA_BASE64}
  