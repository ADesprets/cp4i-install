apiVersion: v1
metadata:
  name: pvc-ldap-main
kind: PersistentVolumeClaim
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: $my_file_ldap_storage_class
  volumeMode: Filesystem
