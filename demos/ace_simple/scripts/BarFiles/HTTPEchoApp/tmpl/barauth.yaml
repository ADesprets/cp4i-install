apiVersion: appconnect.ibm.com/v1beta1
kind: Configuration
metadata:
  labels:
    backup.appconnect.ibm.com/component: configuration
  name: ${VAR_ACE_BARAUTH_NAME}
  namespace: ${VAR_ACE_PROJECT}
spec:
  secretName: ${VAR_ACE_BARAUTH_SECRET_NAME}
  version: ${MY_ACE_VERSION}
  type: barauth